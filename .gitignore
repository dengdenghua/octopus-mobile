# ─────────────── Android / Gradle ───────────────
*.iml
.gradle/
**/build/
/captures
.externalNativeBuild
.cxx
local.properties

# Signing / secrets — never commit
*.jks
*.keystore
key.properties

# Build outputs
*.apk
*.aab
*.ap_
*.dex
*.class

# Kotlin
.kotlin/

# ─────────────── IDE ───────────────
.idea/
*.iws
*.ipr
out/

# ─────────────── Node / Vite (web prototype) ───────────────
node_modules
dist
dist-ssr
*.local

# ─────────────── Runtime data ───────────────
.uploads/

# ─────────────── Logs ───────────────
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ─────────────── OS / Editor ───────────────
.DS_Store
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# octopus account/relay server (python)
server/__pycache__/
server/.venv/
server/*.db
server/.env

# ─────────────── Linux 容器 assets(由 scripts/download-linux-container-assets.sh 下载) ───────────────
# PRoot 二进制 + Alpine rootfs tarball 不进 git 仓库(共 ~6MB),构建前由脚本下载。
# Ubuntu rootfs(28MB)默认不打包进 APK,运行时按需下载到 filesDir;
# 但若用 --ubuntu 选项预下载到 assets(开发测试用),也需排除。
app/src/main/assets/proot/proot-aarch64
app/src/main/assets/proot/alpine-minirootfs.tar.gz
app/src/main/assets/proot/alpine-minirootfs.tar.gz.sha256
app/src/main/assets/proot/ubuntu-base-*.tar.gz
app/src/main/assets/proot/ubuntu-base-*.tar.gz.sha256
